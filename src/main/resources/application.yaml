spring:
  application:
    name: FakeCommerce
  datasource:
    url: jdbc:mysql://mysql:3306/fakecommerce
    username: root
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  devtools:
    restart:
      enabled: true
      poll-interval: 2000
      quiet-period: 1000

